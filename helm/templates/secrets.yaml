apiVersion: v1
kind: Secret
metadata:
  name: {{ include "helm.fullname" . }}
  labels:
    {{- include "chart.labels" . | nindent 4 }}
type: Opaque
data:
{{- range $k, $v := .Values.secret }}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end }}
